<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.may1722.t_go">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.LoginActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.JobBoardActivity" />
        <activity android:name=".ui.ReviewBoardActivity" />
        <activity android:name=".ui.ProfileActivity"
                  android:screenOrientation="portrait">

        </activity>
        <activity android:name=".ui.SignUpActivity" />
        <activity android:name=".ui.CurrentJobs" />
        <activity android:name=".ui.JobSubmitActivity" />
        <activity android:name=".ui.SubmitReviewActivity" />
        <activity android:name=".ui.ChatActivity"
                  android:screenOrientation="portrait" />
    </application>

</manifest>